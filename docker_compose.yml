version: '3'

services:
  redis:
    image: redis
    ports:
      - 6379:6379

  mysql:
    image: mysql
    ports:
      - 3306:3306

  flask_app:
    build: .
    ports:
      - 5000:5000
    depends_on:
      - redis
      - mysql